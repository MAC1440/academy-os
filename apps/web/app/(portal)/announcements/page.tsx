import { AnnouncementsManagement } from '@web/features/announcements';

// Roles: ADMIN. Announcements are published to staff and learner portals.
export default function AnnouncementsPage() {
  return <AnnouncementsManagement />;
}
