import { baseApi, type ApiRecord, unwrap } from "@web/store/api/base-api";
type PinInput = { staffId: string; pin: string }; type KioskSettings = { defaultStaffShiftStart?: string; defaultStaffShiftEnd?: string; graceMinutes?: number; workingDays?: string[] };
export const kioskApi = baseApi.injectEndpoints({ endpoints: (build) => ({ listKioskStaff: build.query<ApiRecord[], string>({ query: (branchId) => `/kiosk/branches/${branchId}/staff`, transformResponse: unwrap }), kioskCheckIn: build.mutation<ApiRecord, { branchId: string; body: PinInput }>({ query: ({ branchId, body }) => ({ url: `/kiosk/branches/${branchId}/check-in`, method: "POST", body }), transformResponse: unwrap }), kioskCheckOut: build.mutation<ApiRecord, { branchId: string; body: PinInput }>({ query: ({ branchId, body }) => ({ url: `/kiosk/branches/${branchId}/check-out`, method: "POST", body }), transformResponse: unwrap }), getKioskSettings: build.query<ApiRecord, void>({ query: () => "/attendance-kiosk/settings", transformResponse: unwrap }), updateKioskSettings: build.mutation<ApiRecord, KioskSettings>({ query: (body) => ({ url: "/attendance-kiosk/settings", method: "PATCH", body }), transformResponse: unwrap }), overrideStaffAttendance: build.mutation<ApiRecord, { branchId: string; attendanceId: string; body: { status?: string; overrideReason?: string; checkOutAt?: string } }>({ query: ({ branchId, attendanceId, body }) => ({ url: `/branches/${branchId}/staff-attendance/${attendanceId}/override`, method: "PATCH", body }), transformResponse: unwrap }) }) });
export const { useListKioskStaffQuery, useKioskCheckInMutation, useKioskCheckOutMutation, useGetKioskSettingsQuery, useUpdateKioskSettingsMutation, useOverrideStaffAttendanceMutation } = kioskApi;
