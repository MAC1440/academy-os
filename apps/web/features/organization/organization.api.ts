import { baseApi, type ApiRecord, unwrap } from "@web/store/api/base-api";
type OrganizationInput = Partial<Pick<ApiRecord, "name" | "logo" | "email" | "phone" | "address">>;
type BranchInput = { name: string; address: string; city?: string; phone?: string; status?: string };
type OperatingHourInput = { label: string; opensAt: string; closesAt: string };
export const organizationApi = baseApi.injectEndpoints({ endpoints: (build) => ({ getOrganization: build.query<ApiRecord, void>({ query: () => "/organization", transformResponse: unwrap, providesTags: ["Organization"] }), updateOrganization: build.mutation<ApiRecord, OrganizationInput>({ query: (body) => ({ url: "/organization", method: "PATCH", body }), transformResponse: unwrap, invalidatesTags: ["Organization"] }), listBranches: build.query<ApiRecord[], void>({ query: () => "/branches", transformResponse: unwrap, providesTags: ["Branch"] }), getBranch: build.query<ApiRecord, string>({ query: (id) => `/branches/${id}`, transformResponse: unwrap, providesTags: ["Branch"] }), createBranch: build.mutation<ApiRecord, BranchInput>({ query: (body) => ({ url: "/branches", method: "POST", body }), transformResponse: unwrap, invalidatesTags: ["Branch"] }), updateBranch: build.mutation<ApiRecord, { branchId: string; body: BranchInput }>({ query: ({ branchId, body }) => ({ url: `/branches/${branchId}`, method: "PATCH", body }), transformResponse: unwrap, invalidatesTags: ["Branch"] }), listOperatingHours: build.query<ApiRecord[], string>({ query: (branchId) => `/branches/${branchId}/operating-hours`, transformResponse: unwrap, providesTags: ["Branch"] }), createOperatingHour: build.mutation<ApiRecord, { branchId: string; body: OperatingHourInput }>({ query: ({ branchId, body }) => ({ url: `/branches/${branchId}/operating-hours`, method: "POST", body }), transformResponse: unwrap, invalidatesTags: ["Branch"] }), updateOperatingHour: build.mutation<ApiRecord, { branchId: string; operatingHourId: string; body: Partial<OperatingHourInput> }>({ query: ({ branchId, operatingHourId, body }) => ({ url: `/branches/${branchId}/operating-hours/${operatingHourId}`, method: "PATCH", body }), transformResponse: unwrap, invalidatesTags: ["Branch"] }) }) });
export const { useGetOrganizationQuery, useUpdateOrganizationMutation, useListBranchesQuery, useGetBranchQuery, useCreateBranchMutation, useUpdateBranchMutation, useListOperatingHoursQuery, useCreateOperatingHourMutation, useUpdateOperatingHourMutation } = organizationApi;
