version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: academy
      POSTGRES_PASSWORD: academy
      POSTGRES_DB: academy_os
    ports:
      - '5432:5432'
